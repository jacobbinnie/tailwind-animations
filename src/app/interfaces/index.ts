export type types = "button" | "gradient" | "space" | "classic";

export type Designs = {
  [key: string]: {
    animation: string;
    keyframes: any;
  };
};

export type Buttons = {
  [key: string]: {
    button: string;
  };
};
